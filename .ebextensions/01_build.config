# .ebextensions/01_build.config
# This command runs 'npm run build' after 'npm install' and before
# the server is started by the Procfile.

container_commands:
  01_run_build:
    command: "npm run build"